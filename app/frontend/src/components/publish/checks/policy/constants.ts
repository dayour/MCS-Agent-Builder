export const APPROVAL_PENDING_SUMMARY = 'Policy requirements met. This agent requires admin approval — it will enter a pending state after publishing.';

export const CONNECTOR_POLICY_PROVENANCE_NOTE = 'Replace this fallback when org policy and connector restriction hooks are available to publish checks.';

export const APPROVAL_STATE_PROVENANCE_NOTE = 'Replace this fallback when real approval-state hooks are available to publish checks.';

export const POLICY_CHECK_PROVENANCE_NOTE = 'Replace provisional policy checks as org-backed policy and approval hooks become available.';